spring:
  datasource:
    url: jdbc:mysql://8.148.235.131:3306/pm_portfolio_hub?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

server:
  port: 8080

# 存储路径配置
storage:
  template:
    path: d:/temp/templates/  # 本地开发环境
    # path: /opt/pm-portfolio/templates/  # Linux服务器环境