package com.pmportfolio.hub.repository;

import com.pmportfolio.hub.model.PmResume;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PmResumeRepository extends JpaRepository<PmResume, Long> {
}
